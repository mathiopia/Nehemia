// let can
let a=0
function setup() {
  let width = windowWidth / 1.5
  let height = windowHeight / 1.1
  createCanvas(width, height)
  // canv.parent('can-container')

  // let sprite = new Sprite()
  // sprite.diameter = 50;

}

function draw() {
  background(0)
  fill(255, 0, 255)
  stroke('cyan')
  strokeWeight(3)
  line(width / 2, height / 2, width / 2 + 100, height / 2)
  line(width / 2 + 150, height / 2, width / 2 + 250, height / 2)






}
